# Project Worklog

---
Task ID: 1
Agent: Z.ai Code (orchestrator)
Task: Build "AR TEMPMAIL" — a fully functional, mobile-friendly temporary email web app as a single self-contained index.html using HTML + Tailwind CSS (CDN) + JavaScript (module), powered by the Mail.tm API, with splash screen, dark/light mode, 10-minute timer, inbox polling, message viewer, and toasts. Serve it through the Next.js preview at `/`.

Work Log:
- Inspected scaffold (src/app/layout.tsx, page.tsx), confirmed dev server running on port 3000 and api.mail.tm reachable (curl 200).
- Wrote `/home/z/my-project/index.html` (~880 lines, single self-contained file): splash screen with animated progress bar, header with theme toggle, Change Mail / Delete Mail buttons, email card with Copy, 10-minute countdown timer card with Extend + progress bar, inbox list (auto-refresh 10s, empty state, unread dots), detail screen (back, avatar, sanitized HTML body, Delete Message), bottom-center toasts (toastIn/toastOut, 3s), dark mode via localStorage.theme + Tailwind dark: classes, Space Grotesk + Material Symbols Outlined, no-scrollbar utility, all spec'd functions (fetchWithTimeout, initApp, getNewEmail, checkInbox, openEmail, copyToClipboard, extendTimer, showToast, escapeHTML, formatDate, showSpinner).
- Discovered critical blocker during browser verification: Mail.tm now sends `Access-Control-Allow-Origin` ONLY to `https://mail.tm` (verified via curl with multiple Origins) — direct browser fetch from any third-party origin (including localhost and file://) is CORS-blocked with "Failed to fetch".
- Solution: app still tries the official `https://api.mail.tm` first (spec-compliant); on TypeError it transparently switches once to a same-origin relay `/api/mailtm` and toasts an info notice.
- Implemented relay: `src/app/api/mailtm/[...path]/route.ts` — transparent proxy forwarding method/headers (Authorization, Content-Type, Accept)/body/query to api.mail.tm, adds permissive CORS headers, 20s timeout, 502 on relay failure, OPTIONS preflight support.
- Rewrote `src/app/page.tsx` to render the app full-viewport via iframe (`/ar-tempmail.html`, allow="clipboard-write"); copied the standalone file to `public/ar-tempmail.html`.
- Hardened API layer after testing: automatic 429 rate-limit retry with 1.5s/3s backoff (Mail.tm throttles rapid account creation); 401 handling regenerates the mailbox automatically.
- Browser-verified end-to-end with agent-browser (mobile 390x844 + desktop 1280x800): splash + progress bar fade, real email generation (multiple addresses), copy toast, dark/light toggle + persistence + icon swap, timer countdown/expiry toast/extend reset, empty inbox state, mock-message inbox rendering (avatar/subject/preview/time/unread dot/count), openEmail fetch + graceful 404 error body, sanitized HTML detail rendering, Delete Message local flow, Change Mail, Delete Mail (account deleted via API then regenerated), sticky footer behavior on both viewports, zero console errors.
- `bun run lint` clean; dev.log shows the full pipeline (domains → accounts 201 → token → messages polling) with 200s.

Stage Summary:
- Deliverable: `/home/z/my-project/index.html` — the ready-to-use single-file app (identical copy served at `public/ar-tempmail.html` for the preview).
- Preview: `/` renders the app full-screen through an iframe; relay route `/api/mailtm/[...path]/route.ts` makes it fully functional despite Mail.tm's CORS lockdown.
- Key decisions: violet/fuchsia gradient design system (no blue/indigo); direct-API-first with transparent relay fallback; 429 backoff retry; local (simulated) message deletion per spec; new mailbox generated on each app start per spec.
- Known limits: receiving a real inbound email could not be exercised from the sandbox (no outbound SMTP); the pipeline was verified through the real /messages list endpoint plus mock-rendered UI. Standalone file:// usage requires any reachable Mail.tm relay (CORS is server-side and cannot be bypassed client-side).

---
Task ID: 2
Agent: Z.ai Code (orchestrator)
Task: v2 upgrade — "next level" modern UI/UX + Saved Mailboxes feature (save a temp email, return later — even next day — restore it and keep receiving OTP/verification emails), plus extra functionality across the app.

Work Log:
- Rewrote index.html (v2.0, ~1700 lines) around a 3-tab mobile-app shell: Mail / Saved / Settings with fixed bottom navigation (safe-area aware, active fill icons, live badges for unread mail and saved count) and animated screen transitions.
- Saved Mailboxes: bookmark toggle on the identity card persists {id, address, password, savedAt, lastUsedAt} to localStorage (ar_saved_mails). Saved screen lists cards with avatar, relative timestamps, copy/remove actions and a gradient "Open mailbox" button that re-logins via POST /token and hot-swaps the active session, then jumps to the inbox. "Active now" state shown for the current address; double-tap "Clear all" in Settings with armed confirmation.
- Session restore: active mailbox persisted (ar_active_session); on load the app re-logins with stored credentials (fresh token) so reloading keeps the same address. "Remember session" setting toggles this (verified OFF → reload generates fresh, ON → reload restores).
- OTP auto-detection: keyword-proximity regex extracts 4-8 digit codes from subject/intro/text/html. Renders dashed copy-chips inside inbox items, a "VERIFICATION CODE DETECTED" banner with big code + Copy button in the detail view, and a tap-to-copy chip inside the new-mail toast. Optional "Auto-copy OTP codes" setting copies the code the instant it arrives; "Vibrate on new mail" adds haptic feedback.
- Settings screen: dark mode switch (mirrors header toggle), remember-session, auto-refresh interval segmented control (5/10/30s — applied live to the running poller), vibrate, auto-copy OTP, saved-count, double-tap clear-all, about footer.
- Inbox upgrades: client-side search (sender/subject/preview/code) with dedicated no-match state; message deletion is now real (DELETE /messages/{id} with local fallback); opened messages marked seen server-side via PATCH (merge-patch+json); 401 handler now first tries silent token re-login before regenerating.
- Share button (Web Share API with copy fallback), decorative background glows, splash ring pulse animation, per-screen enter animations.
- Fixed during verification: theme switch was double-bound to rememberSession (removed stray bindSwitch); OTP chip nested button-inside-button invalid HTML (converted to span[role=button]); delete-message button now receives the message id up front so deletion works even if the body failed to load; restore now auto-switches to the Mail tab.
- Browser-verified (agent-browser, mobile 390x844 + desktop 1280x800, light + dark): save → badge → Saved tab; reload → same mailbox restored; second mailbox save → restore switch; OTP chip + banner + copy; search incl. code search + no-match; settings toggles/interval persistence; clear-all double-tap; single-card removal; delete message; fresh-load on `/` (iframe) with new mailbox; zero console errors; lint clean; dev.log shows full token/accounts/messages pipeline.

Stage Summary:
- Deliverable remains the single self-contained /home/z/my-project/index.html (mirrored to public/ar-tempmail.html for the / preview via iframe; relay /api/mailtm unchanged).
- Headline v2 features: Saved Mailboxes with day-later restore, session restore on reload, OTP auto-detection everywhere (list chip, detail banner, toast, auto-copy), search, share, settings, bottom-tab app shell.
- Storage keys: ar_saved_mails, ar_active_session, ar_settings (+ existing theme). Default settings: autoRefresh 10s, vibrate ON, autoCopyOtp OFF, rememberSession ON.

---
Task ID: 3
Agent: Z.ai Code (orchestrator)
Task: v3 upgrade — user asked for "more features, UI/UX, workflow, security — think it, don't do blindly": evaluate what is genuinely workable/necessary against the current codebase and implement only that.

Work Log:
- Evaluated and REJECTED (with rationale): server DB/Prisma (credentials must stay device-local — stronger privacy, zero attack surface), user auth (anonymous by design), websocket/Mercure push (10s polling is proven; SSE adds CORS uncertainty for no real need), multi-language/extra themes (scope control).
- Attachments (real Mail.tm feature): attachment chips in detail view (type-aware icons, filename, content-type, human size) + authenticated download via new apiBlob() (blob → objectURL → a[download], direct→relay fallback, graceful error toasts); inbox items show a paperclip when hasAttachments. CRITICAL relay fix: route.ts used res.text() which corrupts binaries — switched to res.arrayBuffer() passthrough (JSON still works).
- Safety/workflow: Delete Mail now opens a promise-based confirm sheet ("Delete forever", Escape/backdrop/Cancel); removing a saved mailbox shows a 5s UNDO toast; new-mail toasts gained a "View" action (opens the message).
- Backup & restore: Settings → Export copies a validated {app,version,exportedAt,mailboxes[]} JSON to clipboard; Import sheet parses pasted backups, skips invalid/duplicate entries, reports counts. Honest cross-device story for device-local credentials.
- Notifications: WebAudio two-tone chime (no assets) + gesture unlock, "Sound on new mail" setting (default ON).
- Privacy: "Privacy blur" setting blurs sender/subject/preview/address/saved addresses (body.privacy-blur, stripped on reveal); quick eye toggle in inbox header reveals/hides.
- Inbox: "Read all" button (appears when unread>0; local seen + best-effort server PATCH seen); pull-to-refresh gesture on the Mail screen (touchstart/move/end, 72px threshold, indicator + arrow flip, haptic, guards: scrollY=0 & list at top & single touch).
- Security hardening: sanitizer now also strips formaction/background/poster javascript-ish URLs, removes style attrs with position:fixed/sticky (anti-overlay phishing), adds referrerpolicy=no-referrer to links; address local-part + all randomness now crypto-based (pickRandom with rejection sampling); attachments/backup code never evals input.
- Polish: one-time onboarding tip banner (LS_TIP), Copy text button in detail, favicon (inline SVG gradient), theme-color meta synced with dark mode (#09090b/#7c3aed), prefers-reduced-motion support, aria labels/roles on all new controls, version bumped to v3.0.
- Browser-verified (agent-browser, iframe at /, mobile 390x844 light+dark + desktop 1280x800): init → restore-on-reload keeps same address; tip dismiss persists; save → bookmark_added; delete-confirm sheet cancel keeps mailbox; mock OTP mail → inbox chip + unread dot + Read all; detail OTP banner 482913 + attachment chip (application/pdf · 200 KB) + Copy text; sanitizer: script NOT executed, position:fixed stripped, formaction stripped, rel/referrerpolicy set; dead attachment URL fails gracefully; blur on/off + eye reveal + persistence; chime no-throw; import sheet (1 added / duplicate+invalid skipped) + persisted; undo remove restores entry; export payload shape valid; Change Mail → real POST /token restore of saved mailbox lands back on Mail tab; simulated PTR gesture (indicator 44px, "Release to refresh", reset, refresh fired); search no-match + by-name; dark/light meta theme-color swap; footer pinned both viewports; 0 app console errors (only Tailwind CDN production notice); lint clean; JS syntax + all 78 DOM refs validated; index.html ≡ public/ar-tempmail.html.

Stage Summary:
- Deliverable: /home/z/my-project/index.html v3.0 (2277 lines) mirrored to public/ar-tempmail.html; relay route now binary-safe.
- Headline v3: attachments view+download, confirm-before-destroy + undo, backup export/import, chime + View-action toasts, privacy blur + eye, mark-all-read, pull-to-refresh, hardened sanitizer & crypto randomness, reduced-motion/a11y/favicon/theme-color polish.
- Deliberately not added (rationale recorded): server database, user accounts, websocket push — would add attack surface or complexity without serving the anonymous temp-mail use case.
- Storage keys: ar_saved_mails, ar_active_session, ar_settings (now {autoRefresh, vibrate, sound, autoCopyOtp, blur, rememberSession}), ar_tip_v3_dismissed (+ theme).
- Known limits: real inbound mail still untestable in sandbox (no outbound SMTP) — pipeline verified via /messages endpoints and mock UI; pull-to-refresh verified via synthetic TouchEvents (headless has no real touch).

---
Task ID: 4
Agent: Z.ai Code (orchestrator)
Task: v4 upgrade — "upgrade existing features/functionality, UI/UX, typography, colour palette; must work with current database and codebase." Audit-first approach: only improvements that run on the existing localStorage store, single-file architecture and Mail.tm+relay API.

Work Log:
- Audited v3 (2,278 lines) and scoped by feasibility: KEPT all storage keys (ar_saved_mails, ar_active_session, ar_settings, theme, ar_tip_v3_dismissed), API flows, sanitizer, PTR, attachments, backup; REJECTED new tabs/APIs/themes as bloat.
- Typography system: body/UI switched to Inter, brand/display stays Space Grotesk (new font-display utility on header/splash/screen titles/detail subject), addresses + OTP codes + timer digits now JetBrains Mono; single combined Google Fonts request (3 families).
- Colour palette: new Tailwind tokens — paper (#f5f3fb light / #0b0912 dark, violet-tinted), card (#fff / #16131f), line (#e8e4f2 / #262133) — applied to body, header, nav, every card/sheet/input/list item incl. JS-rendered templates; layered shadow-card + violet glow shadow-pop; seg/switch/OTP-chip literals re-tinted; meta theme-color dark value synced to #0b0912.
- Components: timer bar replaced by 64px conic-gradient progress ring (mono time inside; --p custom prop; warn <5min amber, danger <1min rose + rose digits); deterministic per-sender avatar gradients (avatarClass hash → av-0..av-7, no blue/indigo) in inbox, saved list and detail; bottom-nav animated top pill (.tab-btn.active::before) driven by updateTabUI; inbox list gets branded thin-scroll scrollbar (replaces no-scrollbar per UI standard); toasts rebuilt (icon bubble, mono copy chip); sheets springier pop + darker blurred backdrops.
- Functionality: inbox search now also matches the extracted OTP code (e.g. "482913" finds the mail); Copy button morphs to emerald check_circle for 1.6s on successful copy (gated on copyText result — headless clipboard denial correctly does NOT morph); saved-card address mono'd; version → v4.0 in about line + header comment.
- Fixed mid-implementation: sequential MultiEdit partial-application required re-grepping exact live strings; final pass verified 0 leftover timerBar refs, 0 old zinc surface pairs, JS syntax (node --check) and all 78 $('id') refs resolve.
- Browser-verified (agent-browser, iframe at /): mobile 390x844 light+dark — address mono, ring counting (--p=91→08:23), paper tokens (rgb 245,243,251 / rgb 11,9,18), meta theme sync; save → bookmark_added + toast; Saved tab card (av-6 hashed avatar, mono address, ACTIVE NOW); mock OTP mail → inbox chip (mono) + unread badge; search "482913" finds it, "zzz999" shows no-match; renderDetail → banner 482913 mono + av-7 avatar; Change Mail → real new address; restoreMailbox → real POST /token back to saved address on Mail tab with fresh token; confirm sheet cancel keeps mailbox; toast bubble + code chip; desktop 1280x800 light+dark; session restore across reload; 0 page errors, 0 app console errors (only Next.js/HMR + expected relay-switch warn); lint clean; index.html ≡ public/ar-tempmail.html (2,360 lines); dev.log shows clean /messages 200 polling.

Stage Summary:
- Deliverable: index.html v4.0 (2,360 lines) mirrored to public/ar-tempmail.html; no backend/storage/API changes.
- v4 = pure design-system + micro-interaction upgrade on the proven v3 feature set: Inter/JetBrains-Mono/Space-Grotesk type scale, violet-tinted paper/card/line tokens, conic timer ring, hashed avatar gradients, nav pill, richer toasts/sheets, branded scrollbar, copy morph, search-by-OTP-code.
- Copy-success morph is intentionally gated on clipboard success (verified it does not fire when clipboard is denied).

---
Task ID: 5-a
Agent: general-purpose sub-agent (Task 5-a, SEO/static pages)
Task: Create static crawlable pages + agent-facing files in /home/z/my-project/public/ for SEO / agentic SEO / LLM-friendliness (robots.txt, sitemap.xml, llms.txt, llms-full.txt, manifest.webmanifest, favicon.svg, about/faq/privacy/disclaimer.html). No changes to src/, index.html, ar-tempmail.html or package.json.

Work Log:
- Read worklog.md (Tasks 1-4) for the design system + storage keys; used sandbox date 2026-08-28 for all <lastmod> / "Last updated" fields; canonical host https://artempmail.app everywhere.
- Wrote public/robots.txt EXACTLY per spec (13 user-agent blocks incl. GPTBot/ClaudeBot/PerplexityBot/CCBot etc., Disallow /api/, Sitemap line).
- Wrote public/sitemap.xml: 5 URLs (/, about, faq, privacy, disclaimer .html), lastmod 2026-08-28, changefreq monthly, priority 1.0 for / and 0.8 for the rest.
- Wrote public/manifest.webmanifest EXACTLY per spec (theme #7c3aed, bg #0b0912, 4 icon entries incl. maskable; icon-192/512.png left for the owner agent).
- Wrote public/favicon.svg EXACTLY per spec (violet-500→fuchsia-500 gradient envelope, rx=14, 64px viewBox).
- Wrote public/llms.txt EXACTLY per spec structure (llmstxt.org style: summary blockquote, Pages, Key facts, For machines; lastmod filled with 2026-08-28).
- Wrote public/llms-full.txt (301 lines, 14.4 KB): H1 + summary + page links, then complete verbatim About / FAQ (10 Q&As as `## Q1:`-`## Q10:` + answer) / Privacy Policy / Disclaimer content, plus `## Feature reference` (15 features incl. all spec items), `## Storage keys` markdown table (theme, ar_saved_mails, ar_active_session, ar_settings) and `## Contact` (self-serve → FAQ). Paragraphs hard-wrapped at 100 cols via a /tmp python textwrap pass.
- Built the 4 HTML pages (about/faq/privacy/disclaimer) on a shared shell matching the app design system: Tailwind CDN + inline tailwind.config (darkMode:'class', paper/card/line tokens, shadow-card, Inter/Space Grotesk/JetBrains Mono single Google-Fonts request + Material Symbols Outlined with font-variation-settings), theme boot script in <head> (localStorage 'theme'), sticky blurred header (36px gradient logo box with inline envelope SVG copied from favicon.svg, AR TEMPMAIL gradient wordmark, dark-mode toggle button persisting to localStorage 'theme' with dark_mode/light_mode icon swap, "Open app" button), max-w-2xl main with h1 + 48px violet→fuchsia gradient underline, sections grouped in rounded-2xl shadow-card bordered cards (section class="mt-8 first:mt-0"), violet check-icon lists, numbered violet badges for the 3 how-it-works steps, mono violet chips for localStorage keys in privacy, "Last updated: 2025" muted line under privacy H1, two-row footer (About · FAQ · Privacy Policy · Disclaimer · Open app + © 2025 line with llms.txt link, aria-current="page" on the active link), focus-visible:ring-2 ring-violet-500 on all interactive elements, no blue/indigo anywhere.
- Head metadata per page: unique title/description, canonical, OG (website/AR TEMPMAIL/title/desc/url/og-image.png 1200x630), Twitter summary_large_image, favicon + manifest + theme-color links.
- JSON-LD per page: single ld+json block with @graph = WebPage + BreadcrumbList (Home → current page); faq.html additionally carries a full FAQPage node with all 10 verbatim Q&As.
- Self-check (node /tmp/check5a.mjs + python minidom): all 10 files exist/non-empty; manifest JSON.parse OK; favicon byte-exact vs spec; robots has 13 UA blocks + sitemap line; sitemap = 5 urls, lastmod x5, priorities 1.0/0.8 x4, valid XML; ld+json JSON.parse OK on all 4 pages (faq FAQPage mainEntity = 10); tag balance (section/div/ul/ol/li/span/a/p/h1/h2/article/header/footer/main/nav/button/code) balanced on all 4 pages; theme boot script, fonts, toggle, footer line verified per page; llms-full 301 lines with Q1-Q10 + closing sections; zero blue/indigo classes. git status shows only the 10 target files modified/added.

Stage Summary:
- Deliverables: public/robots.txt (627 B), sitemap.xml (935 B), llms.txt (1.8 KB), llms-full.txt (14.4 KB, 301 lines), manifest.webmanifest (807 B), favicon.svg (544 B), about.html (16.7 KB), faq.html (19.2 KB), privacy.html (18.5 KB), disclaimer.html (13.8 KB) — all at https://artempmail.app/* via canonical URLs.
- Design system identical to the app (violet→fuchsia, paper/card/line tokens, Space Grotesk/Inter/JetBrains Mono, Material Symbols); dark mode shared localStorage 'theme' key; pages are pure static assets — no src/ or app changes.
- Deviations (minor, deliberate): sections use class="mt-8 first:mt-0" so the first section inside each card isn't double-padded (spec asked for mt-8 on sections AND card wrapping); llms-full.txt prefixes FAQ questions Q1-Q10 (`## Q1: …`) to match the "`## Q` / answer" instruction while keeping all 10 verbatim; footer "© 2025" kept verbatim from spec even though sandbox date is 2026; icon-192.png / icon-512.png / og-image.png are referenced but owned/generated by the parent task.

---
Task ID: 6
Agent: Z.ai Code (orchestrator)
Task: Create a separate, attractive and comprehensive intro/landing page for the AR TEMPMAIL web app with "extraordinary animation and style and high visuals", wired as the new entry experience before the app, working with the current codebase.

Work Log:
- Audited worklog (Tasks 1-5a) + live code: app is v5 (index.html 2,639 lines pre-edit) with paper/card/line design tokens (Inter / Space Grotesk / JetBrains Mono, Material Symbols Outlined, violet→fuchsia, localStorage 'theme' default light); static SEO pages (about/faq/privacy/disclaimer.html) and og-image.png already exist from Task 5-a.
- Built /home/z/my-project/public/landing.html (1,043 lines, single self-contained file, no new deps) on the exact Task 5-a design system. Sections: brand nav (anchor links + theme toggle + Open app CTA + mobile hamburger), hero with badge/kinetic word-stagger H1 ("Burner email, ready in one tap." with animated gradient word)/sub/rotating use-case word chip ("Perfect for …"), live app-mock visual with looping OTP demo (mail slides in → digits scramble → settle 493 118 → copy pulse → "Code copied" toast → reset), live-ticking timer chip (09:41 ↓), floating chips (Saved for later / Spam, kept out), scroll cue; use-case marquee (seamless 2-group loop, hover-pause); animated counter stats (0 / 10s / 10min / 100%); bento features grid (6 honest cards incl. live inbox, OTP detection, saved mailboxes, self-destruct ring, privacy, attachments + 4-cell details strip with hairline dividers); 3-step how-it-works with animated dashed connector; 6-item FAQ accordion (grid-rows 0fr→1fr, aria-expanded, single-open); conic-border CTA panel ("Spam has a new enemy."); footer with target="_top" links to about/faq/privacy/disclaimer + data-launch "Open app" + llms.txt.
- Animation system: themed preloader (letter-stagger wordmark + indeterminate bar, body.ready gate, 3.5s fallback), IntersectionObserver .reveal with per-card --d delays, cursor spotlight (pointer:fine, lerped rAF), hero 3D tilt on pointermove (lerp, perspective 1200px), per-card pointer-tracked radial glow, magnetic buttons, button shine sweep, drifting orbs + masked grid + SVG noise + animated gradient text, branded scrollbar, full prefers-reduced-motion fallbacks, :focus-visible rings.
- Integration protocol: landing [data-launch] buttons → parent.postMessage({type:'AR_TEMPMAIL_LAUNCH'}) when embedded, else standalone fallback navigates to /ar-tempmail.html (verified). Theme shares the app's localStorage 'theme' key.
- Rewrote src/app/page.tsx as 'use client' orchestrator: landing iframe always mounted (theme + scroll persist); on AR_TEMPMAIL_LAUNCH mounts /ar-tempmail.html iframe and crossfades to it (700ms opacity); on AR_TEMPMAIL_HOME fades back while the app iframe STAYS MOUNTED (mailbox + polling survive round-trips — verified same address aekof90uk6u@emalupe.com across home→relaunch on desktop and mobile); origin-checked message listener; SEO JSON-LD (@graph WebSite/WebApplication/HowTo/FAQPage) + crawlable <noscript> preserved. Fixed lint (react-hooks/set-state-in-effect) by replacing per-view setState with a one-shot async "revealed" flag.
- App exit hook: index.html brand (logo + wordmark) converted to <a id="brandHome"> (title "Back to intro", focus ring) posting {type:'AR_TEMPMAIL_HOME'} when window.self !== window.top, else smooth-scrolls top; appended after initApp(); mirrored to public/ar-tempmail.html (diff-verified SYNCED).
- Landing head: full SEO set (title/description, canonical to /, OG/Twitter with existing og-image.png, theme-color pair, manifest, favicon), same Tailwind config tokens, WebPage JSON-LD node.
- Fixed during verification: (1) gradient hero word "one tap." was invisible — the .grad-anim animation declaration replaced the wordIn entrance animation on the same element; fixed by nesting the gradient on a child span; (2) details-strip gap-px dividers were invisible — gave the grid a line-colored background and cells card backgrounds; (3) shimmer bars slightly darkened for light mode.
- Browser-verified with agent-browser (desktop 1280×800 + mobile 390×844, light + dark): preloader → hero renders complete H1; rotator cycles; OTP demo loop runs (caught at scramble/copy/toast phases); timer chip ticks; theme toggle dark↔light with localStorage persistence across reload; FAQ opens/closes with correct content; hamburger menu opens with working Open app; launch → crossfade → real mailbox generated (live Mail.tm via relay); app brand → back to landing (landing inert=false, app opacity-0 inert); relaunch → same mailbox preserved; footer Privacy Policy (target=_top) navigates top window to /privacy.html; standalone /landing.html launch falls back to /ar-tempmail.html navigation; screenshots captured at every step; zero page errors, zero unexpected console messages; bun run lint clean; dev.log shows clean /token + /messages 200 pipeline.

Stage Summary:
- Deliverables: public/landing.html (new, 1,043 lines, single-file intro page), src/app/page.tsx (landing⇄app shell with crossfade + persistent app session), index.html + public/ar-tempmail.html (brand-exit hook, app otherwise unchanged, 2,654 lines).
- New user flow: / → landing (intro, features, FAQ, CTA) → "Open app" → the v5 app; app brand tap → back to landing with the live mailbox kept alive in the background.
- PostMessage protocol: AR_TEMPMAIL_LAUNCH (landing→shell), AR_TEMPMAIL_HOME (app→shell); same-origin checked.
- Known minor limits: landing theme toggle and app theme toggle each persist the same 'theme' key, but the already-mounted app iframe reads it at load (its own toggle covers live switching); landing.html is canonicalized to "/" and intentionally not added to sitemap.xml (it IS the "/" experience; Task 5-a files untouched).
